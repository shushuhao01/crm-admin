# CRM 平台管理后台

用于管理私有部署授权和SaaS租户的独立管理后台。

## 功能特性

- 🔐 管理员认证（独立于CRM用户系统）
- 📋 授权码管理（创建、编辑、撤销、续期）
- 📦 版本发布管理（发布新版本、强制更新设置）
- 📊 仪表盘统计（授权统计、验证日志）
- 👥 管理员账户管理（仅超级管理员）

## 技术栈

- Vue 3 + TypeScript
- Element Plus UI
- Pinia 状态管理
- Vue Router
- Axios

## 开发环境

```bash
# 安装依赖
cd admin
npm install

# 启动开发服务器
npm run dev
```

开发服务器运行在 http://localhost:5174

## 生产构建

```bash
npm run build
```

构建产物在 `dist` 目录。

## 后端配置

确保后端 `.env` 文件中启用了管理后台：

```env
ENABLE_ADMIN=true
```

## 初始化管理员

首次使用需要初始化超级管理员账户：

```bash
cd backend
npx ts-node scripts/initAdmin.ts [用户名] [密码] [邮箱]

# 示例（使用默认值）
npx ts-node scripts/initAdmin.ts
# 默认: admin / admin123 / admin@example.com
```

## API 路径

管理后台API路径: `/api/v1/admin/*`

- 认证: `/api/v1/admin/auth/*`
- 授权管理: `/api/v1/admin/licenses/*`
- 版本管理: `/api/v1/admin/versions/*`
- 仪表盘: `/api/v1/admin/dashboard/*`
- 公开验证: `/api/v1/admin/verify/*`

## 部署说明

1. 管理后台应独立部署，不包含在客户交付包中
2. 建议使用独立域名或子路径（如 admin.example.com）
3. 生产环境务必修改默认管理员密码
